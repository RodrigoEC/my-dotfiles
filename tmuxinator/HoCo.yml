# /home/rodrigo/.config/tmuxinator/HoCo.yml

name: HoCo
root: ~/cc/projetos/HoCo

startup_window: editor

windows:
  - Neovim: nvim .
  - Background:
      panes:
        - docker-compose up --build
        - git status
